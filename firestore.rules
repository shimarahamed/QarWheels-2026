rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are completely open and are for debugging purposes only.
    // They should be replaced with secure rules before any production deployment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
