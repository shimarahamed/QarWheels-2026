{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user registered on the QarWheels platform, holding their basic profile information.",
      "properties": {
        "email": {
          "type": "string",
          "description": "The user's email address, used for communication and potentially login. (Relationship: UserProfile 1:N Car)",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The user's first name."
        },
        "lastName": {
          "type": "string",
          "description": "The user's last name."
        },
        "phoneNumber": {
          "type": "string",
          "description": "The user's contact phone number."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "email",
        "firstName",
        "lastName",
        "createdAt",
        "updatedAt"
      ]
    },
    "Car": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Car",
      "type": "object",
      "description": "Represents a vehicle owned by a user, including its core details, VIN information, and current status.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Reference to the UserProfile who owns this car. (Relationship: UserProfile 1:N Car)"
        },
        "vin": {
          "type": "string",
          "description": "Vehicle Identification Number, a globally unique identifier for the car. Used for prefilling car information via external APIs."
        },
        "make": {
          "type": "string",
          "description": "The manufacturer of the car (e.g., 'Toyota', 'BMW')."
        },
        "model": {
          "type": "string",
          "description": "The specific model of the car (e.g., 'Camry', 'X5')."
        },
        "year": {
          "type": "number",
          "description": "The manufacturing year of the car."
        },
        "licensePlate": {
          "type": "string",
          "description": "The car's license plate number."
        },
        "color": {
          "type": "string",
          "description": "The exterior color of the car."
        },
        "engineType": {
          "type": "string",
          "description": "The type of engine (e.g., 'Gasoline', 'Diesel', 'Electric', 'Hybrid')."
        },
        "currentMileage": {
          "type": "number",
          "description": "The current recorded mileage of the car."
        },
        "lastMileageUpdateDate": {
          "type": "string",
          "description": "The date and time when the current mileage was last updated.",
          "format": "date-time"
        },
        "purchaseDate": {
          "type": "string",
          "description": "The date when the user acquired the car.",
          "format": "date-time"
        },
        "imageUrl": {
          "type": "string",
          "description": "URL to an image of the car, typically stored in Firebase Storage.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the car record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the car record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "vin",
        "make",
        "model",
        "year",
        "currentMileage",
        "lastMileageUpdateDate",
        "createdAt",
        "updatedAt"
      ]
    },
    "Vendor": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Vendor",
      "type": "object",
      "description": "Represents a service provider or parts store registered on the QarWheels platform, including their location and approval status.",
      "properties": {
        "ownerId": {
          "type": "string",
          "description": "The UID of the user who owns and manages this vendor profile."
        },
        "name": {
          "type": "string",
          "description": "The official name of the garage or parts store."
        },
        "type": {
          "type": "string",
          "description": "The type of vendor (e.g., 'Garage', 'Parts Store', 'Both')."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the vendor's services or offerings."
        },
        "address": {
          "type": "string",
          "description": "The physical street address of the vendor's location."
        },
        "city": {
          "type": "string",
          "description": "The city where the vendor is located."
        },
        "country": {
          "type": "string",
          "description": "The country where the vendor is located (e.g., 'Qatar')."
        },
        "phoneNumber": {
          "type": "string",
          "description": "The main contact phone number for the vendor."
        },
        "email": {
          "type": "string",
          "description": "The main contact email address for the vendor.",
          "format": "email"
        },
        "websiteUrl": {
          "type": "string",
          "description": "The official website URL for the vendor.",
          "format": "uri"
        },
        "latitude": {
          "type": "number",
          "description": "The geographical latitude coordinate of the vendor's location, for map display."
        },
        "longitude": {
          "type": "number",
          "description": "The geographical longitude coordinate of the vendor's location, for map display."
        },
        "status": {
          "type": "string",
          "description": "The approval status of the vendor ('Pending Approval', 'Approved', 'Rejected', 'Active', 'Inactive')."
        },
        "rating": {
          "type": "number",
          "description": "The average customer rating from 1 to 5."
        },
        "reviewCount": {
          "type": "number",
          "description": "The total number of reviews received."
        },
        "imageId": {
          "type": "string",
          "description": "Reference to an image placeholder ID."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the vendor record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the vendor record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "ownerId",
        "name",
        "type",
        "address",
        "city",
        "country",
        "phoneNumber",
        "email",
        "status",
        "createdAt",
        "updatedAt"
      ]
    },
    "ServiceRecord": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ServiceRecord",
      "type": "object",
      "description": "Details of a historical service performed on a specific car, including vendor information and invoice links.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Reference to the UserProfile who this service record belongs to. Used for security rules."
        },
        "carId": {
          "type": "string",
          "description": "Reference to the Car that received the service. (Relationship: Car 1:N ServiceRecord)"
        },
        "vendorId": {
          "type": "string",
          "description": "Reference to the Vendor who performed the service. (Relationship: Vendor 1:N ServiceRecord)"
        },
        "serviceType": {
          "type": "string",
          "description": "The general category of service performed (e.g., 'Oil Change', 'Brake Service', 'Tire Rotation')."
        },
        "serviceDescription": {
          "type": "string",
          "description": "A detailed description of the work performed during the service."
        },
        "serviceDate": {
          "type": "string",
          "description": "The date and time when the service was completed.",
          "format": "date-time"
        },
        "mileageAtService": {
          "type": "number",
          "description": "The car's mileage at the time the service was performed."
        },
        "cost": {
          "type": "number",
          "description": "The total cost of the service."
        },
        "invoiceUrl": {
          "type": "string",
          "description": "URL to the invoice document for this service, stored in Firebase Storage.",
          "format": "uri"
        },
        "notes": {
          "type": "string",
          "description": "Any additional notes or comments about the service."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the service record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the service record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "carId",
        "vendorId",
        "serviceType",
        "serviceDate",
        "mileageAtService",
        "cost",
        "createdAt",
        "updatedAt"
      ]
    },
    "ServicePrediction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ServicePrediction",
      "type": "object",
      "description": "An AI-generated prediction for future car maintenance needs, linked to a specific car.",
      "properties": {
        "carId": {
          "type": "string",
          "description": "Reference to the Car for which the prediction was made. (Relationship: Car 1:N ServicePrediction)"
        },
        "predictedServiceType": {
          "type": "string",
          "description": "The type of service predicted (e.g., 'Oil Change', 'Tire Replacement', 'Brake Inspection')."
        },
        "predictedDueDate": {
          "type": "string",
          "description": "The estimated date when the predicted service will be due.",
          "format": "date-time"
        },
        "predictedMileage": {
          "type": "number",
          "description": "The estimated mileage at which the predicted service will be due."
        },
        "reasoning": {
          "type": "string",
          "description": "A detailed explanation from the AI model about why this service is predicted."
        },
        "confidenceScore": {
          "type": "number",
          "description": "A numerical score (0.0-1.0) indicating the AI model's confidence in this prediction."
        },
        "predictionTimestamp": {
          "type": "string",
          "description": "The date and time when this service prediction was generated.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the prediction (e.g., 'Open', 'Actioned', 'Dismissed')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the service prediction record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the service prediction record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "carId",
        "predictedServiceType",
        "predictedDueDate",
        "predictedMileage",
        "reasoning",
        "confidenceScore",
        "predictionTimestamp",
        "status",
        "createdAt",
        "updatedAt"
      ]
    },
    "Booking": {
      "title": "Booking",
      "type": "object",
      "description": "Represents a service appointment made by a user with a vendor.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "ID of the user who made the booking."
        },
        "vendorId": {
          "type": "string",
          "description": "ID of the vendor the booking is with."
        },
        "vendorName": {
          "type": "string",
          "description": "Denormalized name of the vendor for easy display."
        },
        "carId": {
          "type": "string",
          "description": "ID of the user's car for the service."
        },
        "serviceName": {
          "type": "string",
          "description": "The name of the service booked."
        },
        "bookingDate": {
          "type": "string",
          "format": "date-time",
          "description": "The date and time of the appointment."
        },
        "status": {
          "type": "string",
          "enum": [
            "Confirmed",
            "Completed",
            "Cancelled"
          ]
        },
        "cost": {
          "type": "number",
          "description": "The estimated or final cost of the service."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the booking was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the booking was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "vendorId",
        "vendorName",
        "carId",
        "serviceName",
        "bookingDate",
        "status",
        "createdAt",
        "updatedAt"
      ]
    },
    "Service": {
      "title": "Service",
      "type": "object",
      "description": "Represents a single service offered by a vendor.",
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "price": { "type": "number" },
        "duration": { "type": "number", "description": "Duration in minutes." }
      },
      "required": ["name", "description", "price", "duration"]
    },
    "InventoryItem": {
      "title": "InventoryItem",
      "type": "object",
      "description": "Represents a single item in a vendor's inventory.",
      "properties": {
        "name": { "type": "string" },
        "sku": { "type": "string" },
        "stock": { "type": "number" },
        "price": { "type": "number" },
        "supplier": { "type": "string" }
      },
      "required": ["name", "sku", "stock", "price", "supplier"]
    },
    "StaffMember": {
      "title": "StaffMember",
      "type": "object",
      "description": "Represents a staff member at a vendor's establishment.",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["Technician", "Service Advisor", "Admin"] },
        "status": { "type": "string", "enum": ["Active", "Inactive"] }
      },
      "required": ["name", "email", "role", "status"]
    },
    "Promotion": {
      "title": "Promotion",
      "type": "object",
      "description": "Represents a promotional offer from a vendor.",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "code": { "type": "string" },
        "discount": { "type": "string" },
        "startDate": { "type": "string", "format": "date-time" },
        "endDate": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["Active", "Expired", "Scheduled"] }
      },
      "required": ["title", "code", "discount", "startDate", "endDate", "status"]
    },
    "Review": {
      "title": "Review",
      "type": "object",
      "description": "Represents a customer review for a vendor.",
      "properties": {
        "userId": { "type": "string" },
        "customerName": { "type": "string" },
        "rating": { "type": "number" },
        "comment": { "type": "string" },
        "service": { "type": "string" },
        "date": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "customerName", "rating", "comment", "date"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual user profiles. Access is restricted to the user whose ID matches '{userId}'."
        }
      },
      {
        "path": "/users/{userId}/cars/{carId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/Car"
          },
          "description": "Stores details for each car owned by a user. This is a subcollection of a user's profile."
        }
      },
      {
        "path": "/users/{userId}/cars/{carId}/serviceRecords/{serviceRecordId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/ServiceRecord"
          },
          "description": "Contains historical service records for a specific car. Nested under a user's car."
        }
      },
      {
        "path": "/users/{userId}/cars/{carId}/servicePredictions/{servicePredictionId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/ServicePrediction"
          },
          "description": "Stores AI-generated service predictions for a specific car. Nested under a user's car."
        }
      },
      {
        "path": "/vendors/{vendorId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/Vendor"
          },
          "description": "Stores information about service providers and parts stores. Public users can only read 'Approved' vendors."
        }
      },
       {
        "path": "/vendors/{vendorId}/services/{serviceId}",
        "definition": {
          "schema": { "$ref": "#/backend/entities/Service" },
          "description": "Services offered by a specific vendor."
        }
      },
      {
        "path": "/vendors/{vendorId}/inventory/{itemId}",
        "definition": {
          "schema": { "$ref": "#/backend/entities/InventoryItem" },
          "description": "Inventory items for a specific vendor."
        }
      },
      {
        "path": "/vendors/{vendorId}/staff/{staffId}",
        "definition": {
          "schema": { "$ref": "#/backend/entities/StaffMember" },
           "description": "Staff members of a specific vendor."
        }
      },
      {
        "path": "/vendors/{vendorId}/promotions/{promotionId}",
        "definition": {
          "schema": { "$ref": "#/backend/entities/Promotion" },
          "description": "Promotions for a specific vendor."
        }
      },
      {
        "path": "/vendors/{vendorId}/reviews/{reviewId}",
        "definition": {
          "schema": { "$ref": "#/backend/entities/Review" },
          "description": "Customer reviews for a specific vendor."
        }
      },
      {
        "path": "/roles_admin/{adminId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "A collection used for Database Access Control (DBAC). The existence of a document at this path where '{adminId}' matches a user's UID grants administrative privileges to that user."
        }
      },
      {
        "path": "/bookings/{bookingId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/Booking"
          },
          "description": "Stores all service bookings. Access is granted to the user who owns the booking, the vendor of the booking, or an admin."
        }
      }
    ]
  }
}
  